node_modules
.DS_Store

# Generated build output, published to Cloudflare Pages by CI (see ARCHITECTURE.md).
# Nothing here is committed — it's all regenerated by scripts/build-registry.mjs.
public
registry.json

# Cached upstream responses for the aggregation step (scripts/aggregate.mjs).
# Local-only speedup; CI checks out clean and always fetches fresh.
.cache
